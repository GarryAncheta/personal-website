import { AppBar } from '@material-ui/core';
import { Toolbar } from '@material-ui/core';
import './Navbar.css';

function Navbar()
{
    return(
        <AppBar>
            <Toolbar className = 'background'>
                BRIANNE CHITWOOD you suck :)
            </Toolbar>
        </AppBar>
    );
}

export default Navbar;